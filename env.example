# Database (Neon PostgreSQL)
# Get this from Neon dashboard: https://console.neon.tech
# Format: postgresql://user:password@host/database?sslmode=require
# For connection pooling (recommended for serverless), use the pooled connection string
DATABASE_URL=postgresql://user:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require

# Cloudinary Configuration
# Get these from Cloudinary dashboard: https://console.cloudinary.com
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Next.js
NODE_ENV=production

# Optional: Base URL for your application (if needed for absolute URLs)
# NEXT_PUBLIC_BASE_URL=https://your-domain.vercel.app

